/**
 * @file main.cpp
 * @brief Punto de entrada del programa base. Solo contiene llamadas a funciones sin logica interna.
 */
#include "App.h"

int main() {
    // Se ejecutara todo el programa mediante esta sola funcion
    ejecutarApp();

    return 0;
}
